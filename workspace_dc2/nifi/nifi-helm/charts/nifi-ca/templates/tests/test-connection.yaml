apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nifi-ca.fullname" . }}-test-connection"
  labels:
    {{- include "nifi-ca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nifi-ca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
