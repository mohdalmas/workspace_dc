apiVersion: v1
kind: Service
metadata:
  labels:
    app: schema-registry
  name: schema-registry
spec:
  ports:
  - port: 8445
    protocol: TCP
    targetPort: 8445
  selector:
    app: schema-registry
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
