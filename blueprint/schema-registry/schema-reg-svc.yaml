apiVersion: v1
kind: Service
metadata:
  labels:
    app: schema-registry
  name: schema-registry
spec:
  ports:
  - port: 9099
    protocol: TCP
    targetPort: 9099
  selector:
    app: schema-registry
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
